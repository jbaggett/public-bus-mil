[project]
name = "public-bus-mil"
version = "1.0.0"
description = "Reproducible exam-level MIL test set from public BUS video datasets (MICCAI-BUV + WHBUS)"
readme = "README.md"
requires-python = ">=3.9"
license = {text = "MIT"}
dependencies = [
    "numpy>=1.23",
    "pillow>=9.0",
    "scipy>=1.10",
]

[project.optional-dependencies]
torch = ["torch>=2.0"]

[project.scripts]
build-bus-mil-bags = "build_bags:main"

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
include = ["public_bus_mil*"]
